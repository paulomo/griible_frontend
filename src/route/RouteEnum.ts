export enum WelcomeRoutes {
  base = '/',
  home = '/welcome',
  terms = '',
}

export enum AuthRoutes {
  login = "/login",
  signup = "/signup",
  forgotpassword = "/forgot-password"
}

export enum AccountRoutes {
  connectAccount = "/connect-new-account",
  email = "/my-email/account"
}